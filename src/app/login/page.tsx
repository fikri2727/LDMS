import { LoginDesignV2 } from "@/components/login/LoginDesignV2";

/**
 * The previous design is kept at @/components/login/LoginDesignV1 (unused,
 * not deleted) in case it's ever needed again.
 */
export default function LoginPage() {
  return <LoginDesignV2 />;
}
